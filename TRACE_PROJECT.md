# TRACE — Project Tracker
**Last Updated:** June 2026  
**Repo:** https://github.com/RevanthBoina/TRACE  
**Purpose:** Anti-piracy platform — creators upload videos, get invisibly watermarked, register platform links, system monitors 24/7 for unauthorized copies.

---

## Architecture Decisions

| Component | Planned | Actual | Why Changed |
|-----------|---------|--------|-------------|
| Compute | Oracle VM | AWS EC2 t3.micro | Oracle free tier reducing June 15 2026 |
| Object Storage | Oracle Object Storage | AWS S3 | Already configured with credentials |
| Database | PostgreSQL on Oracle VM | Neon.tech (testing) → AWS RDS (production) | Fastest way to unblock testing |
| Watermarking | VideoSeal full install | VideoSeal TorchScript JIT (y_256b_img.jit) | No pip install needed, just PyTorch |
| Frontend | Next.js on Vercel | Next.js on Vercel | Unchanged |
| Background Jobs | Celery + Redis | Celery + Redis | Not yet implemented |

---

## S3 Buckets
- `trace-upload-temp` — raw video uploads (deleted after watermarking)
- `trace-watermarked-out` — watermarked video output (signed URL, 15min expiry)

---

## Environment Variables (.env)
Stored in **AWS Parameter Store** under `/trace/*`  
Keys:
- `AWS_ACCESS_KEY_ID`
- `AWS_SECRET_ACCESS_KEY`
- `AWS_REGION` = us-east-1
- `S3_UPLOAD_BUCKET` = trace-upload-temp
- `S3_OUTPUT_BUCKET` = trace-watermarked-out
- `DATABASE_URL` = Neon.tech PostgreSQL URL
- `RESEND_API_KEY`

---

## VideoSeal Model
- File: `backend/ckpts/y_256b_img.jit` (218MB)
- Downloaded from: `https://dl.fbaipublicfiles.com/videoseal/y_256b_img.jit`
- **NOT in git** (gitignored) — downloaded by `setup.sh` every fresh session
- Uses TorchScript — no `pip install videoseal` needed
- Runs on CPU (ARM compatible)
- 256-bit watermark payload per video

---

## Fresh Session Setup
Every new session run this from project root:
```bash
chmod +x setup.sh && ./setup.sh
```
This does:
1. Pulls all secrets from AWS Parameter Store → writes `.env`
2. Downloads VideoSeal model if missing

**Requirement:** AWS CLI must be configured (`aws configure`) with valid credentials.

---

## Project Structure
```
TRACE/
├── app/                        # Next.js frontend (Vercel)
│   ├── api/
│   │   ├── upload/route.ts     # ⚠️ OLD — to be removed, replaced by FastAPI
│   │   └── health/route.ts     # ⚠️ OLD — to be removed
│   ├── page.tsx
│   └── layout.tsx
├── backend/                    # FastAPI Python backend (→ EC2)
│   ├── ckpts/
│   │   └── y_256b_img.jit      # VideoSeal model (gitignored)
│   ├── workers/
│   │   └── watermark_worker.py # WatermarkWorker class ✅
│   ├── config.py               # Settings from .env
│   ├── database.py             # Neon PostgreSQL connection
│   └── main.py                 # FastAPI app, /upload, /health
├── components/
│   ├── video-uploader.tsx
│   └── registration-panel.tsx
├── setup.sh                    # Fresh environment setup ✅
├── store_secrets.sh            # One-time secret push to AWS ✅
├── requirements.txt
├── .env                        # Gitignored — generated by setup.sh
└── vercel.json
```

---

## What's Built ✅
- S3 upload endpoint (raw video → `trace-upload-temp`)
- Duplicate detection via SHA-256 hash
- PostgreSQL connection via Neon
- Job ID generation (UUID v4)
- VideoSeal JIT model downloaded and tested
- WatermarkWorker class built (`watermark_worker.py`)
- AWS Parameter Store for secrets
- setup.sh for fresh environment recovery
- EC2 t3.micro instance launched (AWS free tier 6 months)

---

## What's NOT Built Yet ❌
- WatermarkWorker NOT wired into upload endpoint (biggest gap)
- Celery + Redis background job queue
- Signed download URL after watermarking
- Anti-piracy registration endpoint (Step 6 in blueprint)
- Fingerprint verification worker (Step 7)
- Bot scanner (Step 8)
- Match notification system (Step 9)
- DINOv2 perceptual fingerprinting
- Frontend wired to FastAPI (currently calls its own Next.js API routes)
- Next.js API routes cleanup (app/api/ should be deleted)
- CORS updated for Vercel domain

---

## EC2 Instance
- Type: t3.micro (AWS free tier, 6 months)
- OS: Ubuntu 22.04 LTS
- Region: us-east-1
- Ports open: 22 (SSH), 80 (HTTP), 443 (HTTPS), 8000 (FastAPI)
- Status: Launched — not yet configured
- **Next action:** SSH in, install dependencies, deploy FastAPI backend

---

## Immediate Next Steps (In Order)
1. Wire WatermarkWorker into `/upload` endpoint in `main.py`
2. Add Celery + Redis for background processing
3. Add signed download URL endpoint
4. SSH into EC2 → deploy FastAPI backend
5. Update Next.js CORS to point at EC2 public IP
6. Delete `app/api/` — Next.js API routes are dead code
7. Switch DATABASE_URL from Neon to AWS RDS on EC2

---

## Key Rules
- **Never store video files** — only fingerprints and metadata in DB
- **Free tier watermark:** 1 in 10 frames (test with all frames first, optimize later)
- **Paid tier watermark:** 5 in 10 frames
- **Detection logic:** 2 of 3 signals must match (VideoSeal + DINOHash + audio)
- **Scan frequency:** YouTube every 6hrs, Instagram every 12hrs
